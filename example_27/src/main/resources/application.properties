# Disables template caching during development so changes show immediately
# No need for disabling it while using Devtools as it disables the cache
#spring.thymeleaf.cache=false

spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# If there is no customized user config -> default credentials "username: user" and the "password is randomly generated" and printed in the console
# In real-world we don't configure users as below

spring.security.user.name=eazybytes
spring.security.user.password=12345